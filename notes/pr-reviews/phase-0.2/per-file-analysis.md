# Per-File Analysis

## Instructions

Read `raw-diff.md` for diff context, then fill in the judgment fields below.

**Do NOT modify the `### Facts` table.** These values come from file-manifest.json and are authoritative — they cannot be changed.

---

## File: .claude/hooks/current_task_TASK-PREP.json

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +24 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

New hook configuration JSON recording TASK-PREP metadata, task description, acceptance commands, and the full task ID list for the phase-0.2 execution pipeline.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — static JSON config.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — infrastructure file for the TOML plan execution pipeline.

### Notes

Plain JSON file with no executable code. Contains all 11 task IDs (TASK-PREP through TASK-10) and their ordering. `acceptance_commands` field carries `cargo check` validation.

---

## File: Cargo.lock

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +286 |
| Lines removed | -1 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Auto-generated lockfile update adding transitive dependencies for `tempfile` dev-dependency (fastrand, getrandom, cfg-if, bitflags, errno, libc, windows-sys, r-efi, wasip2, wasip3) and a hashbrown version bump.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — auto-generated file.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — consequence of adding `tempfile` dev-dependency in Cargo.toml.

### Notes

Auto-generated by Cargo. New entries: tempfile 3.23, fastrand 2.4.1, getrandom 0.4.2, id-arena 2.3.0, hashbrown 0.15.5 (new version alongside existing 0.17.0). No unexpected or suspicious dependency additions.

---

## File: Cargo.toml

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +3 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Added `tempfile = "3"` as a dev-dependency for unit and integration tests that need temporary directories.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — dependency declaration.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — corresponds to TASK-PREP in the compiled manifest.

### Notes

Tempfile is a well-established, minimal dependency. Only declared as dev-dependency so it does not affect the production binary. Consistent with the project's pragmatic crate selection principle (tempfile is the standard choice for this use case).

---

## File: execution_reports/.checkpoint_phase-0.2.json

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +18 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Checkpoint file tracking task completion/failure status during phase-0.2 execution, recording base commit and which tasks passed (TASK-2, TASK-3) vs. which initially failed and were retried.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — execution tracking artifact.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — execution pipeline artifact.

### Notes

TASK-4 through TASK-10 listed as "failed" — these required retries after fixes (compilation/import ordering). TASK-1 not listed (likely completed between TASK-2/TASK-3 pass and TASK-4 failure). File is execution infra, not production code.

---

## File: execution_reports/execution_phase-0.2_20260428.md

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +288 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Execution report documenting the outcome of each task run during the phase-0.2 implementation pipeline, including validation output, compilation errors, and fix attempts.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — execution report document.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — execution pipeline artifact.

### Notes

Documents TASK-2 and TASK-3 passing cleanly, TASK-1 passing (listed twice), TASK-4 initially failing due to stale imports (`Error` and `Result` no longer used in file_parser.rs), TASK-5 failing (compilation errors from prior-task dependency), followed by retry/fix cycles. The duplicate TASK-2 entry is a report formatting artifact. Records that the eventual cleanup commit (bbc0490) resolved the extraction bug in the integration test.

---

## File: notes/plan-enrichment/phase-0.2/codebase-state.md

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +328 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | Error, Result, Config, ErrorEntry, find_workspace_root, enumerate_members, resolve_crate_roots, parse_cargo_toml, parse_file, extract_public_items, extract_imports, extract_re_exports, extract_submodules, extract_impls, resolve_module_path, build_module_tree, compute, PublicItem, kind_to_string, render_json, render_to_writer, Cli, main, run, relativize_path, test_sample_workspace_output, test_deterministic_output, test_missing_path_exits_nonzero |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Pre-implementation snapshot documenting every source file mentioned in the phase plan — their public API signatures, module wiring, and gaps vs. the plan.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — documentation artifact.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — plan enrichment pipeline output.

### Notes

Documents 10 files (schema.rs, workspace.rs, file_parser.rs, module_tree.rs, lib.rs, cargo_info.rs, cross_refs.rs, render.rs, main.rs, integration_test.rs) with full API surface, module wiring, and plan-vs-current gap analysis. Each section includes a "Plan relationship" subsection that explicitly states what needs to change. This is a reference document for the plan enrichment process.

---

## File: notes/plan-enrichment/phase-0.2/deferred-and-patterns.md

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +11 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Lists 5 deferred improvements from phase 0.1 (empty errors vector, silent error swallowing, path fallback bugs, missing unit tests) — all of which phase 0.2 goals absorb.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — documentation artifact.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — plan enrichment pipeline input.

### Notes

File was modified in the current branch (per git status: `M notes/plan-enrichment/phase-0.2/deferred-and-patterns.md`), indicating deferred items have been resolved or updated during execution. All 5 deferred items map to phase-0.2 goals: error wiring (Goal 1), path fallbacks (Goal 3), unwrap_or_default removal (Goals 3, 5), unit tests (Goal 2).

---

## File: notes/plan-enrichment/phase-0.2/draft-elaboration.md

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +563 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | ErrorSeverity, ErrorContext, ErrorEntry, parse_file, ParsedFile, SynParseError, build_parse_error_entry, build_module_tree |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Draft elaboration of 15 change items for phase-0.2, providing proposed type signatures, module placement, error handling strategy, ownership/lifetime notes, and trait coherence analysis for each.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — documentation artifact.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — plan enrichment pipeline intermediate output.

### Notes

Each item includes: proposed type signature (as Rust code), module placement, error handling strategy, ownership/lifetime notes, and trait coherence notes. Grounded in existing patterns from codebase-state.md. The elaboration follows a consistent template across all 15 items. No new patterns invented — all derive from existing codebase conventions (bon::Builder, serde rename_all, thiserror, Option skip_serializing_if).

---

## File: notes/plan-enrichment/phase-0.2/gather-summary.md

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +27 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Gather summary for the plan enrichment process: 10 tasks created, all 5 deferred items absorbed, 10/10 before-block checks confirmed, 2 wiring issues flagged.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — documentation artifact.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — plan enrichment pipeline output.

### Notes

Has a duplicated paragraph ("Before-block verification: 10/10 confirmed / Unverified tasks: none / Wiring issues flagged: 2" appears twice). Minor formatting artifact from the enrichment pipeline. Two flagged wiring issues: (1) exact clippy lint locations need runtime verification, (2) orphaned module handling retains placeholder ModuleInfo alongside ErrorEntry per existing pattern.

---

## File: notes/plan-enrichment/phase-0.2/plan.approved.toml

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +2168 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | FileInfo, ErrorSeverity, ErrorContext, Result, ErrorEntry, parse_file, ParsedFile, SynParseError, build_module_tree, resolve_module_path, find_workspace_root, enumerate_members, run, relativize_path, extract_public_items, extract_imports, extract_re_exports, extract_submodules, extract_impls, render_json, crate, kind_to_string, parse_source, parse_file_returns_ast_for_valid_source, parse_file_returns_error_for_invalid_source, parse_file_returns_empty_for_empty_file, extract_public_items_finds_struct_enum_trait_fn, extract_public_items_empty_for_no_public_items, extract_imports_finds_use_statements, extract_re_exports_finds_pub_use, extract_re_exports_finds_rename, extract_submodules_finds_mod_declarations, extract_submodules_marks_cfg_test, extract_impls_finds_fn_type_const, build_parse_error_entry_constructs_error, resolve_module_path_finds_rs_file, resolve_module_path_finds_mod_rs, resolve_module_path_returns_none_for_missing, build_module_tree_returns_empty_for_nonexistent, write_cargo_toml, setup_crate, find_workspace_root_finds_cargo_toml, enumerate_members_returns_members, enumerate_members_returns_err_for_missing_workspace, enumerate_members_applies_exclude, resolve_crate_roots_detects_lib, resolve_crate_roots_detects_bin, parse_cargo_toml_parses_minimal, parse_cargo_toml_uses_defaults_for_missing_package, parse_cargo_toml_distinguishes_deps, make_crate, compute_finds_cross_crate_import, compute_empty_for_no_cross_references, render_to_writer, make_minimal_map, render_json_produces_valid_json, render_json_skips_empty_errors, render_to_writer_matches_render_json, test_missing_path_exits_nonzero, run_binary, parse_output, test_parse_failure_error_entry, test_missing_workspace_section, test_glob_member_patterns, test_workspace_with_exclude, test_deeply_nested_modules, test_reexport_chains, Secret, test_output_via_flag |
| Modified functions | — |
| Removed functions | — |
| Added imports | std::path::Path, rayon::prelude::*, super::*, crate::schema::{Import, ReExport, SubmoduleDecl}, std::path::PathBuf, crate::schema::ErrorEntry, std::io::Write, crate::schema::{ModuleInfo, PublicItem, SubmoduleDecl} |

### Intent

The approved enriched TOML plan containing 11 tasks (TASK-PREP through TASK-10), each with before/after code blocks, acceptance commands, dependency chains, and module wiring checks.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — plan document (TOML).
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — this IS the approved plan.

### Notes

Contains base64-encoded before/after blocks for deterministic application via sd. Each task entry includes: id, file, type, description, changes count, before block, after block, index, is_create flag, acceptance commands, depends_on, before-block verification grep string, and module wiring checks. 2168 lines total. The function names listed in the Facts table are derived from code blocks in the plan (types, functions, and test function names).

---

## File: notes/plan-enrichment/phase-0.2/task-checklist.md

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +274 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Task-by-task checklist for phase-0.2 covering module wiring checks, known failure mode checks, before-block verification, dependency analysis, and notes for all 10 tasks.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — documentation artifact.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — plan enrichment pipeline output.

### Notes

Each task entry includes: module wiring check (pub mod / pub use / consumer co-location), known failure mode check (missing pub mod risk / missing pub use risk / stale import risk), before-block check (grep confirmation), acceptance commands, dependencies, and notes. TASK-4 correctly identifies a high stale import risk — the file_parser.rs imports depend on TASK-1 and TASK-3 being applied first, which the execution report confirms was a real failure mode.

---

## File: plans/compiled/TASK-1.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-1: Add ErrorSeverity enum and ErrorContext struct to schema.rs, using base64-decoded before/after blocks applied via sd.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard compiled task pattern: imports base64 + json + subprocess + sys + Path, decodes STEPs from JSON, applies each via sd with before/after matching. Uses `sd -F -A -n 1` for fixed-string, atomic, single-replacement application. Target: src/schema.rs. One replace step (Type::replace, index 0).

---

## File: plans/compiled/TASK-1.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-1.py: sets strict mode, prints task description, runs the Python runner, then runs acceptance command.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard pattern: `set -euo pipefail`, echo description, run Python runner, run acceptance command. All 12 .sh files follow this identical template.

---

## File: plans/compiled/TASK-10.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-10: Expand integration tests from 3 to 10 covering error cases, workspace patterns, and output.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: tests/integration_test.rs. One replace step adding test functions after the existing `test_missing_path_exits_nonzero`.

---

## File: plans/compiled/TASK-10.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-10.py with acceptance command `cargo test -p rust-workspace-map --test integration_test`.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern.

---

## File: plans/compiled/TASK-2.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-2: Add MissingWorkspaceSection variant to Error enum in schema.rs.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: src/schema.rs. One replace step: adds `MissingWorkspaceSection` variant between GlobPattern and the closing brace of the Error enum. Passed on first execution per the checkpoint file.

---

## File: plans/compiled/TASK-2.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-2.py with acceptance command `cargo check -p rust-workspace-map`.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern.

---

## File: plans/compiled/TASK-3.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-3: Expand ErrorEntry struct with severity, kind, context, and cause fields in schema.rs.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: src/schema.rs. Dependent on TASK-1 (ErrorSeverity and ErrorContext types must exist). Passed on first execution per the checkpoint file.

---

## File: plans/compiled/TASK-3.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-3.py with acceptance command `cargo check -p rust-workspace-map`.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern.

---

## File: plans/compiled/TASK-4.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-4: Change parse_file to return ParsedFile with optional parse errors instead of Result.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: src/file_parser.rs. Initially failed due to stale imports (old `Error` and `Result` symbols no longer used). The execution report shows this was applied atomically with TASK-5. Dependent on TASK-1 and TASK-3 per the task checklist.

---

## File: plans/compiled/TASK-4.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-4.py with acceptance command noting atomic application with TASK-5.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern. Acceptance command is a comment (not `cargo check`) since TASK-4 and TASK-5 must be applied together for compilation to succeed — the module_tree changes in TASK-5 are needed to consume the new ParsedFile type.

---

## File: plans/compiled/TASK-5.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-5: Refactor module_tree.rs — fix path fallbacks, add error collection, change build_module_tree return type (12 changes).

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: src/module_tree.rs. 12 replace steps — the most changes of any single task. Covers: resolve_module_path doc and inline format, build_module_tree signature change, process_submodule orphan handling, process_module_items signature, process_module_info signature, cycle detection, path fallback fixes throughout.

---

## File: plans/compiled/TASK-5.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-5.py with acceptance command `cargo check -p rust-workspace-map`.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern.

---

## File: plans/compiled/TASK-6.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-6: Update workspace.rs enumerate_members to return MissingWorkspaceSection error (3 changes).

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: src/workspace.rs. Three replace steps: errors doc comment on find_workspace_root, errors doc + match-based MissingWorkspaceSection return on enumerate_members, #[must_use] on resolve_crate_roots. Dependent on TASK-2 (MissingWorkspaceSection variant must exist in Error enum).

---

## File: plans/compiled/TASK-6.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-6.py with acceptance command `cargo check -p rust-workspace-map`.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern.

---

## File: plans/compiled/TASK-7.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-7: Refactor lib.rs run() — parallel error collection, error entry construction, remove unwrap_or_default (3 changes).

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: src/lib.rs. Three replace steps: remove crate-level clippy allows + add ErrorSeverity import, change run() body from filter_map to map collecting errors, change run(config) to run(&config). Wait — the clippy allow removal is part of TASK-8, not TASK-7. The diff confirms TASK-7 removes allows and TASK-8 handles individual lint fixes. The checkpoint shows TASK-7 failed initially (likely because TASK-7/TASK-8 overlap in lib.rs).

---

## File: plans/compiled/TASK-7.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-7.py with acceptance command `cargo check -p rust-workspace-map`.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern.

---

## File: plans/compiled/TASK-8.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-8: Remove all crate-level clippy allow attributes and fix individual lint violations (21 changes across multiple files).

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: multiple files (lib.rs for crate-level allows, file_parser.rs, cross_refs.rs, module_tree.rs, workspace.rs for individual fixes). 21 changes total. Acceptance command: `cargo clippy -p rust-workspace-map -- -D warnings` — stricter than other tasks. Applied clippy fixes: inline format args, let chains, redundant closures, String::new() to "", ToString::to_string, #[must_use] annotations.

---

## File: plans/compiled/TASK-8.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-8.py with acceptance command `cargo clippy -p rust-workspace-map -- -D warnings`.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern. Uses clippy with `-D warnings` for acceptance.

---

## File: plans/compiled/TASK-9.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-9: Add unit tests for file_parser, module_tree, workspace, cargo_info, cross_refs, and render (6 changes).

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: 6 files (file_parser.rs, module_tree.rs, workspace.rs, cargo_info.rs, cross_refs.rs, render.rs). Adds test modules to the end of each file. Acceptance: `cargo test -p rust-workspace-map`.

---

## File: plans/compiled/TASK-9.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-9.py with acceptance command `cargo test -p rust-workspace-map`.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern.

---

## File: plans/compiled/TASK-PREP.py

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +44 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled Python runner for TASK-PREP: Add tempfile dev-dependency to Cargo.toml.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — mechanically generated runner script.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Target: Cargo.toml. One replace step appending `[dev-dependencies]` section with `tempfile = "3"`. No dependencies — runs first in the execution sequence per the manifest.json ordering.

---

## File: plans/compiled/TASK-PREP.sh

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +7 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Bash wrapper for TASK-PREP.py with acceptance command `cargo check -p rust-workspace-map`.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — shell wrapper.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Standard bash wrapper pattern.

---

## File: plans/compiled/manifest.json

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +139 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Compiled manifest listing all 11 tasks with metadata: id, script path, runner path, target file, change type, change count, description, and acceptance commands.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — execution manifest.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — compiled from phase-0.2.toml.

### Notes

Tasks ordered: TASK-PREP, TASK-1 through TASK-10. Each task has type "replace" (no creates). Change counts range from 1 (TASK-PREP, TASK-1-4, TASK-10) to 21 (TASK-8). The `skipped` array is empty — all tasks were executed.

---

## File: plans/phase-0.2.toml

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +2168 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | FileInfo, ErrorSeverity, ErrorContext, Result, ErrorEntry, parse_file, ParsedFile, SynParseError, build_module_tree, resolve_module_path, find_workspace_root, enumerate_members, run, relativize_path, extract_public_items, extract_imports, extract_re_exports, extract_submodules, extract_impls, render_json, crate, kind_to_string, parse_source, parse_file_returns_ast_for_valid_source, parse_file_returns_error_for_invalid_source, parse_file_returns_empty_for_empty_file, extract_public_items_finds_struct_enum_trait_fn, extract_public_items_empty_for_no_public_items, extract_imports_finds_use_statements, extract_re_exports_finds_pub_use, extract_re_exports_finds_rename, extract_submodules_finds_mod_declarations, extract_submodules_marks_cfg_test, extract_impls_finds_fn_type_const, build_parse_error_entry_constructs_error, resolve_module_path_finds_rs_file, resolve_module_path_finds_mod_rs, resolve_module_path_returns_none_for_missing, build_module_tree_returns_empty_for_nonexistent, write_cargo_toml, setup_crate, find_workspace_root_finds_cargo_toml, enumerate_members_returns_members, enumerate_members_returns_err_for_missing_workspace, enumerate_members_applies_exclude, resolve_crate_roots_detects_lib, resolve_crate_roots_detects_bin, parse_cargo_toml_parses_minimal, parse_cargo_toml_uses_defaults_for_missing_package, parse_cargo_toml_distinguishes_deps, make_crate, compute_finds_cross_crate_import, compute_empty_for_no_cross_references, render_to_writer, make_minimal_map, render_json_produces_valid_json, render_json_skips_empty_errors, render_to_writer_matches_render_json, test_missing_path_exits_nonzero, run_binary, parse_output, test_parse_failure_error_entry, test_missing_workspace_section, test_glob_member_patterns, test_workspace_with_exclude, test_deeply_nested_modules, test_reexport_chains, Secret, test_output_via_flag |
| Modified functions | — |
| Removed functions | — |
| Added imports | std::path::Path, rayon::prelude::*, super::*, crate::schema::{Import, ReExport, SubmoduleDecl}, std::path::PathBuf, crate::schema::ErrorEntry, std::io::Write, crate::schema::{ModuleInfo, PublicItem, SubmoduleDecl} |

### Intent

The full compiled TOML plan for phase-0.2 — the authoritative plan document from which all compiled scripts were generated. Identical in structure to plan.approved.toml.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — plan document (TOML).
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — this IS the compiled plan.

### Notes

This is the source TOML plan at the canonical path (`plans/phase-0.2.toml`). The `notes/plan-enrichment/phase-0.2/plan.approved.toml` is a copy placed in the notes directory during the enrichment process. Content is effectively identical. The function names listed in Facts are derived from code blocks and test function names within the plan's base64-encoded before/after blocks.

---

## File: src/cargo_info.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +65 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | write_cargo_toml, parse_cargo_toml_parses_minimal, parse_cargo_toml_uses_defaults_for_missing_package, parse_cargo_toml_distinguishes_deps |
| Modified functions | — |
| Removed functions | — |
| Added imports | super::*, std::io::Write |

### Intent

Added 3 unit tests for `parse_cargo_toml`: minimal parsing, defaults for missing package section, and dependency classification (normal vs dev vs workspace members).

### Checklist

- Unnecessary clone/unwrap/expect? Yes — `write_cargo_toml` helper uses `.unwrap()` on `File::create` and `write_all`. Acceptable since it is a test-only helper in `#[cfg(test)]` module.
- Error handling: Test helpers use unwrap — standard for test code where failure should panic.
- Dead code or unused imports? No
- New public API: tests present? Yes — the new functions are the tests themselves. `write_cargo_toml` is test-private.
- Change appears within plan scope? Yes — Goal 2 (Unit Test Suite) and Goal 3 (Path Safety Fixes — `parse_cargo_toml` doc comment addition).

### Notes

Test helper `write_cargo_toml` is duplicated identically in `workspace.rs` tests and `integration_test.rs`. This is acceptable code duplication for test isolation. The `parse_cargo_toml_distinguishes_deps` test correctly verifies that `workspace = true` dependencies appear in `workspace_members` rather than `normal`/`dev` lists. The doc comment addition on `parse_cargo_toml` (`# Errors` section) fulfills the `clippy::missing_errors_doc` lint after crate-level suppression removal.

---

## File: src/cross_refs.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +82 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | make_crate, compute_finds_cross_crate_import, compute_empty_for_no_cross_references |
| Modified functions | — |
| Removed functions | — |
| Added imports | super::*, crate::schema::{CrateType, Import, ItemKind, ModuleInfo, PackageInfo, PublicItem} |

### Intent

Added 2 unit tests for `compute` (cross-crate reference detection) and a `#[must_use]` annotation on `kind_to_string`.

### Checklist

- Unnecessary clone/unwrap/expect? No — test helper `make_crate` uses builder pattern with owned values, no unwrap/expect.
- Error handling: Not applicable — test functions do not involve error paths.
- Dead code or unused imports? No
- New public API: tests present? Yes — both tests verify cross-crate import detection and empty case.
- Change appears within plan scope? Yes — Goal 2 (Unit Test Suite).

### Notes

`compute_finds_cross_crate_import` manually adds an `Import` to the engine module to simulate cross-crate reference — good test design that avoids dependency on the full parse pipeline. `compute_empty_for_no_cross_references` makes a defensive assertion using `||` — either empty types map OR all imported_by lists are empty — which covers edge cases. The `#[must_use]` addition on `kind_to_string` addresses the `clippy::must_use_candidate` lint after crate-level suppression removal.

---

## File: src/file_parser.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +255 |
| Lines removed | -49 |
| Trailing newline | YES |
| Added functions | ParsedFile, SynParseError, parse_file, flatten_use_tree, parse_source, COUNTER, parse_file_returns_ast_for_valid_source, parse_file_returns_error_for_invalid_source, parse_file_returns_empty_for_empty_file, extract_public_items_finds_struct_enum_trait_fn, extract_public_items_empty_for_no_public_items, extract_imports_finds_use_statements, extract_re_exports_finds_pub_use, extract_re_exports_finds_rename, extract_submodules_finds_mod_declarations, extract_submodules_marks_cfg_test, extract_impls_finds_fn_type_const, build_parse_error_entry_constructs_error |
| Modified functions | — |
| Removed functions | parse_file, flatten_use_tree |
| Added imports | super::*, std::path::PathBuf, std::sync::atomic::{AtomicU64, Ordering} |

### Intent

Changed `parse_file` from returning `Result<(syn::File, FileInfo)>` to returning `ParsedFile` (always succeeds, parse errors become data). Added `ParsedFile` and `SynParseError` structs. Added `build_parse_error_entry` helper. Applied clippy fixes (inline format args, let chains, redundant closures). Added 10 unit tests.

### Checklist

- Unnecessary clone/unwrap/expect? No — production code uses no unwrap/expect. Test helper `parse_source` uses `.unwrap()` on `std::fs::write` and `AtomicU64::fetch_add` for unique temp file names — standard test pattern.
- Error handling: Well-structured — `ParsedFile` separates successful AST from optional parse errors. File read errors and syn parse errors are both captured as `SynParseError` data inside `ParsedFile` rather than aborting. The `build_parse_error_entry` helper constructs structured `ErrorEntry` with severity, kind, file, line, and message. Good separation between detection (`parse_file`) and error construction (`build_parse_error_entry`).
- Dead code or unused imports? No
- New public API: tests present? Yes — `ParsedFile` and `SynParseError` are `pub` (crate-visible); 10 tests cover: valid parse, invalid parse, empty file, public item extraction (struct/enum/trait/fn), no-public-items case, imports, re-exports including rename case, submodule declarations, cfg(test) marking, impl extraction, and error entry construction.
- Change appears within plan scope? Yes — Goal 1 (Error Visibility Pipeline — parse failures become structured data). Also addresses Goal 5 clippy fixes.

### Notes

`build_parse_error_entry` is `pub(crate)` — correctly scoped for internal use by `module_tree.rs`. The `parse_source` helper uses an `AtomicU64` counter for unique temp file names to avoid collisions in parallel test execution — good defensive coding. The `extract_re_exports_from_tree` function had a significant behavioral fix: `ReExport.import_path` was previously set to the parent prefix only, now it includes the full path (e.g., `crate::foo as bar` instead of just `crate`). This is visible in the diff at lines 7687-7711 and matches the `re_exports_finds_rename` test. The let-chain refactoring for doc attribute extraction (lines 7619-7637) is cleaner and eliminates 3 levels of nesting.

---

## File: src/lib.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +56 |
| Lines removed | -41 |
| Trailing newline | YES |
| Added functions | run |
| Modified functions | — |
| Removed functions | run |
| Added imports | — |

### Intent

Removed 9 crate-level `#![allow(clippy::*)]` attributes. Changed `run()` signature to borrow Config. Replaced filter_map with map for parallel error collection — each crate now returns `(Option<CrateInfo>, Vec<ErrorEntry>)` instead of silently dropping failures. Added structured error entries for toml parse failures and missing crate roots.

### Checklist

- Unnecessary clone/unwrap/expect? No — no clone/unwrap/expect in production code.
- Error handling: Significantly improved. Replacements: `eprintln!` -> structured `ErrorEntry` with `kind = "toml_parse_error"` and `cause` field; silent `return None` -> `ErrorEntry` with `kind = "missing_crate_roots"` and `severity = Warning`; `unwrap_or_default()` on module_tree results -> explicit `(modules, errors)` destructuring with error aggregation. The `#[allow(clippy::too_many_lines)]` is a targeted suppression rather than crate-wide.
- Dead code or unused imports? No
- New public API: tests present? Not applicable — `run()` is tested via integration tests. The signature change (`config: &Config`) is reflected in `main.rs`.
- Change appears within plan scope? Yes — Goal 1 (Error Visibility Pipeline), Goal 3 (unwrap_or_default removal), Goal 5 (clippy suppression removal).

### Notes

The function now processes member_dirs with `.par_iter().map(...)` returning `Vec<(Option<CrateInfo>, Vec<ErrorEntry>)>` then collects results in a separate pass: crates with parse/root errors produce `None` + error entries, successful crates produce `Some(CrateInfo)` + their module errors. Lost crates are dropped with their errors added to the output errors list. This is the correct behavior per the "partial results" architectural decision — errors from failed crates are collected and visible downstream even though the crate info is absent from the map. The import of `ErrorSeverity` was added to support construction of severity-tagged error entries.

---

## File: src/main.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +1 |
| Lines removed | -1 |
| Trailing newline | YES |
| Added functions | — |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Changed `rust_workspace_map::run(config)` to `rust_workspace_map::run(&config)` to match the new borrowed Config parameter in lib.rs.

### Checklist

- Unnecessary clone/unwrap/expect? No
- Error handling: Not applicable — single-line call site change.
- Dead code or unused imports? No
- New public API: tests present? Not applicable
- Change appears within plan scope? Yes — consequence of run() signature change in lib.rs (Goal 1 / TASK-7).

### Notes

Minimal, correct change. Passing by reference avoids an unnecessary move of Config, which contains PathBuf fields that would otherwise need cloning if Config were used after the call.

---

## File: src/module_tree.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +146 |
| Lines removed | -59 |
| Trailing newline | YES |
| Added functions | build_module_tree, resolve_module_path_finds_rs_file, resolve_module_path_finds_mod_rs, resolve_module_path_returns_none_for_missing, build_module_tree_returns_empty_for_nonexistent |
| Modified functions | — |
| Removed functions | build_module_tree |
| Added imports | crate::schema::{ErrorContext, ErrorEntry, ErrorSeverity, FileInfo, ModuleInfo, SubmoduleDecl}, super::* |

### Intent

Changed `build_module_tree` from `Result<Vec<ModuleInfo>>` to `(Vec<ModuleInfo>, Vec<ErrorEntry>)` — errors are collected alongside modules rather than aborting. Fixed 3 path fallback bugs (`unwrap_or_else(|| Path::new("."))` -> `unwrap_or(crate_root)` / `unwrap_or(file_path)`). Replaced `eprintln!` orphaned module warnings with structured `ErrorEntry` with `ErrorContext`. Added `#[must_use]` on public functions. Added 4 unit tests.

### Checklist

- Unnecessary clone/unwrap/expect? Yes — `process_module_info` calls `errors.clone()` at its return point. The errors are collected via `&mut errors` during the function body, then cloned for the return value. This is correct but incurs O(depth * errors) clone cost across recursive module tree traversal. The original `errors` vector (borrowed) goes out of scope in the caller after the return, so the clone is necessary for the current function signature. Consider returning `(Vec<ModuleInfo>, Vec<ErrorEntry>)` without the `&mut` parameter in a future refactor.
- Error handling: Well-structured. Three error collection points: (1) parse errors on root/child files become `ErrorEntry` with `kind = "syn_parse_error"`, (2) orphaned modules (missing AST item or missing file) become `ErrorEntry` with `kind = "orphaned_module"` and `ErrorContext` containing `module_path`, (3) cycle detection returns empty results silently (correct — no error needed). The dual return (modules + errors) correctly implements the "partial results" pattern: an orphaned module still produces a placeholder `ModuleInfo` with `<unresolved>` file path.
- Dead code or unused imports? No
- New public API: tests present? Yes — `build_module_tree` return type changed (tested via `build_module_tree_returns_empty_for_nonexistent`). `resolve_module_path` is `pub` — 3 unit tests cover .rs file, mod.rs, and missing cases.
- Change appears within plan scope? Yes — Goal 1 (Error Visibility Pipeline), Goal 3 (Path Safety Fixes), Goal 5 (clippy fixes including #[must_use] annotations and inline format args).

### Notes

The `resolve_module_path` inline format change (`format!("{}.rs", mod_name)` -> `format!("{mod_name}.rs")`) is a clippy fix. Path fallback from `Path::new(".")` to the parent file's own path (`crate_root` / `file_path`) corrects a real bug: when `.parent()` returns None (root filesystem), using `"."` could produce incorrect relative paths. Now the fallback uses the crate root itself, which is already absolute. The `#[allow(clippy::too_many_arguments)]` on `process_module_info` is targeted and justified — the function genuinely needs all 8 parameters to operate recursively. The `super::*` import is used for test module access to parent functions.

---

## File: src/render.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +78 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | make_minimal_map, render_json_produces_valid_json, render_json_skips_empty_errors, render_to_writer_matches_render_json |
| Modified functions | — |
| Removed functions | — |
| Added imports | super::* |

### Intent

Added `# Errors` doc comments on `render_json` and `render_to_writer`. Added 3 unit tests covering valid JSON output, empty errors serialization skip, and render_to_writer parity with render_json.

### Checklist

- Unnecessary clone/unwrap/expect? No — tests use `.unwrap()` on `serde_json::from_str` and `String::from_utf8`, which is standard for test assertions.
- Error handling: Doc comments properly document that both functions return `serde_json::Result`. This fulfills the `clippy::missing_errors_doc` lint.
- Dead code or unused imports? No
- New public API: tests present? Yes — 3 tests for the two public functions.
- Change appears within plan scope? Yes — Goal 2 (Unit Test Suite) and Goal 5 (clippy fixes).

### Notes

The `make_minimal_map` helper constructs a minimal but valid `WorkspaceMap` using the builder pattern — good test design that avoids depending on the full parse pipeline. `render_json_skips_empty_errors` correctly tests the `#[serde(skip_serializing_if)]` annotation on the `errors` field. `render_to_writer_matches_render_json` verifies output parity between the string and writer render paths.

---

## File: src/schema.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +41 |
| Lines removed | -1 |
| Trailing newline | YES |
| Added functions | ErrorSeverity, ErrorContext |
| Modified functions | — |
| Removed functions | — |
| Added imports | — |

### Intent

Added `ErrorSeverity` enum (Error, Warning) and `ErrorContext` struct (crate_name, module_path, line, snippet). Expanded `ErrorEntry` with `severity`, `kind`, `context`, `cause` fields. Added `MissingWorkspaceSection` variant to `Error` enum. Fixed a doc comment typo (backtick-quoted `module_tree`).

### Checklist

- Unnecessary clone/unwrap/expect? No — pure type definitions, no runtime logic.
- Error handling: `ErrorSeverity` is `Copy + Clone` — cheap to include in every ErrorEntry. `ErrorContext` uses `bon::Builder` with all `Option<T>` fields and `Default` — follows the established schema pattern. All new fields on `ErrorEntry` have correct serde annotations: `context` and `cause` are `skip_serializing_if = "Option::is_none"`, `severity` and `kind` are required (always serialized). `ErrorContext` fields all have `skip_serializing_if = "Option::is_none"` for compact JSON output.
- Dead code or unused imports? No
- New public API: tests present? Not directly — `ErrorSeverity` and `ErrorContext` are consumed by other modules' tests (file_parser, module_tree, lib.rs). Integration tests exercise the full serialized output including these types.
- Change appears within plan scope? Yes — Goal 1 (Error Visibility Pipeline). Corresponds to TASK-1, TASK-2, TASK-3.

### Notes

The `MissingWorkspaceSection` variant is the only net-new `Error` variant — used by `workspace::enumerate_members` to signal a missing `[workspace]` section rather than silently defaulting to empty members. This is a behavioral change that surfaces a previously-silent configuration error. The serde rename_all conventions are consistent: `ErrorSeverity` uses `snake_case` (serializes as `"error"`/`"warning"`), `ErrorContext` uses `camelCase` (serializes as `"crateName"`, `"modulePath"`), matching the rest of the schema. The doc comment fix (`module_tree` -> `` `module_tree` ``) is a minor improvement.

---

## File: src/workspace.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +120 |
| Lines removed | -11 |
| Trailing newline | YES |
| Added functions | write_cargo_toml, setup_crate, find_workspace_root_finds_cargo_toml, enumerate_members_returns_members, enumerate_members_returns_err_for_missing_workspace, enumerate_members_applies_exclude, resolve_crate_roots_detects_lib, resolve_crate_roots_detects_bin |
| Modified functions | — |
| Removed functions | — |
| Added imports | super::*, std::io::Write |

### Intent

Changed `enumerate_members` to return `Err(Error::MissingWorkspaceSection)` when `[workspace]` section is absent (previously `unwrap_or_default()` returned empty members). Added `# Errors` doc comments on `find_workspace_root` and `enumerate_members`. Added `#[must_use]` on `resolve_crate_roots`. Added 6 unit tests.

### Checklist

- Unnecessary clone/unwrap/expect? Yes — test helpers `write_cargo_toml` and `setup_crate` use `.unwrap()` on `File::create`, `write_all`, `create_dir_all`, `std::fs::write`, and `writeln!`. Acceptable for `#[cfg(test)]` helpers where failure should panic.
- Error handling: `enumerate_members` now correctly surfaces missing `[workspace]` as a hard error rather than silently returning an empty member list. This is a breaking behavior change for callers that previously relied on the default-empty behavior. The new `match` block is explicit and readable. No `unwrap_or_default` remains in the function.
- Dead code or unused imports? No
- New public API: tests present? Yes — 6 tests: workspace root discovery, member enumeration, missing workspace section error, exclude filtering, lib root detection, bin root detection.
- Change appears within plan scope? Yes — Goal 3 (Path Safety Fixes — the `unwrap_or_default` removal). Corresponds to TASK-6.

### Notes

`enumerate_members_returns_err_for_missing_workspace` correctly uses `match` to pattern-match on `Error::MissingWorkspaceSection` rather than `assert!(result.is_err())` — more precise assertion. The `resolve_crate_roots` doc comment was moved below the test module rather than being inside it — the function definition (lines 8662-8666 after tests) is correctly placed after the test block, though the diff appears to show the doc comment relocated. The `#[must_use]` on `resolve_crate_roots` addresses a clippy lint and is correct — callers should always use the return value.

---

## File: tests/integration_test.rs

### Facts (from file-manifest.json — authoritative, do not modify)

| Property | Value |
|----------|-------|
| Lines added | +265 |
| Lines removed | -0 |
| Trailing newline | YES |
| Added functions | run_binary, parse_output, write_cargo_toml, setup_crate, test_parse_failure_error_entry, test_missing_workspace_section, test_glob_member_patterns, test_workspace_with_exclude, test_deeply_nested_modules, test_reexport_chains, Secret, test_output_via_flag |
| Modified functions | — |
| Removed functions | — |
| Added imports | std::io::Write |

### Intent

Expanded integration tests from 3 to 10, adding 7 new tests covering: parse failure error entries, missing workspace section, glob member patterns, exclude filtering, deeply nested modules, re-export chains, and output-via-flag. Added 4 test helpers.

### Checklist

- Unnecessary clone/unwrap/expect? Yes — test helpers use `.unwrap()` on `File::create`, `write_all`, `create_dir_all`, `std::fs::write`, `file_name()`, `to_str()`, `serde_json::from_str`, `std::fs::read_to_string`. Also `.expect("failed to execute binary")` in `run_binary`. All acceptable for test code — panics on setup failure surface the problem immediately.
- Error handling: Not applicable — test code uses unwrap/expect by design.
- Dead code or unused imports? Yes — `use std::io::Write;` is imported at file level but also duplicated inside the `write_cargo_toml` function body (line 8891). The inner `use` is redundant given the top-level import already exists.
- New public API: tests present? Not applicable — this IS the test file.
- Change appears within plan scope? Yes — Goal 4 (Integration Test Expansion) from 3 to 10 tests. Corresponds to TASK-10.

### Notes

The 7 new tests provide good coverage: `test_parse_failure_error_entry` verifies the error visibility pipeline end-to-end (invalid Rust syntax produces `ErrorEntry` with `kind = "syn_parse_error"` in JSON output), `test_missing_workspace_section` verifies non-zero exit for missing `[workspace]`, `test_glob_member_patterns` tests `members = ["crates/*"]` glob resolution, `test_workspace_with_exclude` verifies exclude filtering, `test_deeply_nested_modules` validates 3-level module tree construction (lib.rs -> foo/mod.rs -> bar/mod.rs -> baz.rs), `test_reexport_chains` tests `pub use inner::Secret` re-export discovery, `test_output_via_flag` verifies `-o` output file matches stdout. `Secret` is a struct defined inside an inline module within the test's source string (line 8897-8899), not a separate function — it appears in the Facts table because it's a named type within the test code.

